{
  "name": "revolt-x-school",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json && node scripts/validate-frontends.mjs && node --test test/*.smoke.mjs",
    "start": "node dist/server.js",
    "test": "npm run build"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/helmet": "^13.0.1",
    "dotenv": "^16.6.1",
    "fastify": "^5.6.1",
    "pg": "^8.16.3",
    "zod": "^4.1.11",
    "typescript": "^5.9.2",
    "@types/node": "^24.5.2",
    "@types/pg": "^8.15.5"
  },
  "devDependencies": {
    "tsx": "^4.20.6"
  }
}
